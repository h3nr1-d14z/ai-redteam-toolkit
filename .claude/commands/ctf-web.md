Solve web CTF challenge: $ARGUMENTS

1. **Recon**: View source, check robots.txt, examine cookies, headers, JavaScript files
2. **Technology**: Identify framework, language, template engine, database from errors and responses
3. **Injection testing**: SQLi, XSS, SSTI, SSRF, LFI/RFI, command injection, XXE — test all input points
4. **Auth bypass**: Default creds, JWT manipulation, cookie tampering, type juggling, race conditions
5. **Logic flaws**: Parameter pollution, mass assignment, IDOR, insecure deserialization, path traversal
6. **Source code review**: If source provided, review for hardcoded secrets, vulnerable dependencies, logic bugs
7. **Solve and document**: Write `solve.py` and `writeup.md` with flag

Save to `ctf/<platform>/<challenge>/`
